/* OPERADORES STRING
Os operadores de string só podem ser usados com strings (texto). O +operador junta duas strings. 
Ao contrário, o +=operador é usado para anexar uma string a uma existente. 
Exemplo:

+     Une duas strings                  

+ =   Acrescenta outro a uma string   

*/

var primeiroTxt = "Hello";
var segundoTxt = "world";
var terceiroTxt = "!";

var uniao = primeiroTxt + segundoTxt;

console.log(uniao);
